events {
    worker_connections  1024;
}

http {
    server {
        listen 80;
        root /usr/share/nginx/html;

        location /api/ {
          proxy_pass http://backend:80;
        }

        location / {
        }
    }
}